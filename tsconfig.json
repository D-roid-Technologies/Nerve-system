{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "./", // This sets the base folder to the root folder
    "paths": {
      "@supabaseClient": ["supabaseClient.js"]
    }
  }
}
